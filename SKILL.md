---
name: pm-draw
description: 根据粗需求编写和迭代精简功能 PRD，提出带选项的决策问题；调研竞品页面截图，依据审查通过的 PRD 生成层级清晰的低保真 HTML 原型方案，并按页面标记、问题和建议局部修改。适用于新功能设计和旧功能迭代。
---

# PM Draw

帮助产品经理把背景目标、功能逻辑和操作路径补全为 PRD，再形成可反馈的原型方案。旧功能迭代可先访问页面、截图并记录旧逻辑；竞品截图用于设计参考。无需产品走查、走查计划或回归报告。

`<SKILL>` 是本文件所在目录。脚本依赖 Node.js 22+，无需 npm 安装。产物放用户指定目录，未指定时用 `designs/<product>/`。需求理解、研究与设计由 Agent 完成；脚本只校验、渲染和保存反馈。

## 协作规则

- 根据本次输入选择工作，不要求连续完成所有步骤。用户可以只写 PRD、只回答问题、从已通过的 PRD 出图，或只修改 HTML 中某一页的反馈；沿用已有资料、决策和稳定编号。
- 默认写精简功能 PRD。业务规则、范围和关键交互不明确时给出问题与选项，由用户决策；不把 AI 建议写成已确认需求。普通排版与措辞自行处理。
- PRD 的「已审查通过」只来自用户明确确认，可沿用当前版本已有的通过记录，不因跨轮对话要求重批。回答决策问题不等于批准整份 PRD。
- 业务变化先更新 PRD，受影响版本回到草稿 / 待决策。用户要求预览时可同步输出标有「仅供讨论」的草稿原型；正式方案等 PRD 审查通过。布局、层级、错字等不改变业务含义的修改可直接重建原型。
- 用截图支持观察，用 PRD 支持设计。旧页面事实、竞品做法、用户决定和 AI 建议分清；静态截图无法证明的行为标待确认。竞品功能不自动成为本产品需求。
- 低保真线框分 PC 与移动端两种样式。先从用户要求或现有页面确认目标终端；无法确定时给“PC／移动端／两端”选项。两端都需要时分别设计页面状态，保持业务规则一致；不能把 PC 缩小当作移动端。按参考截图或目标设备设画布尺寸，导航、侧栏、内容区与固定底部区的位置关系准确。
- 原型画布内只画产品实际内容。设备／状态说明、组件编号、PRD 来源、交互结果说明、跳转去向、待确认与反馈放在画布外，不让旁注、徽章或额外箭头改变组件位置；只有产品自身的提示文案留在画布内。
- 简明输出，层级为「功能流程 → 页面状态 → 原型与说明」。只写影响理解、实现或决策的内容，无关模块省略，不重复说明自明组件，不新增流程平台、任务调度或审批系统。

## 按当前任务工作

### 写或完善 PRD

读取 [references/prd-writing.md](references/prd-writing.md)。新功能用背景目标、逻辑和操作路径补全；旧功能用用户描述及实际页面证据整理「旧逻辑 → 新逻辑 → 影响范围」。需要访问旧页面时读取 [references/browser-capture.md](references/browser-capture.md)，在用户已授权范围内直接研究，不另设截图计划确认。

写 `PRD-<product>.md`。将影响业务、流程和验收的未知事项集中为稳定的 Q 编号，每题给 2–3 个可执行选项、影响和有依据的建议。对话中集中提出本轮关键问题，继续完善不依赖答案的内容。用户回答后同时更新正文和决策记录；审查通过后代写状态与日期。

### 从 PRD 生成原型方案

先读完整 PRD，确认当前版本的审查状态。默认依据「已审查通过」版本绘制；用户明确要求草稿预览时按草稿处理。

1. **研究参考**：读取 [references/browser-capture.md](references/browser-capture.md)，选择与本次关键交互相关的少量竞品页面，实际查看截图，记录来源、观察和具体借鉴点。通常 1–3 个产品即可，不写泛泛竞品报告。已有相关证据可复用，仅在过时或范围变化时补充。
2. **组织方案**：读取 [references/wireframe-guide.md](references/wireframe-guide.md) 和 [references/flow-format.md](references/flow-format.md)。按用户路径提取需求原文和页面状态，创建 `mode: prototype`、`target.kind: none` 的 `flow.json`。旧页面与竞品截图写入可选 `references`，不启用 review 模式。
3. **绘制与说明**：每屏标明 `device` 和画布尺寸，按终端绘制低保真原型，写清关键组件行为、数据从哪里来／如何更新／影响哪里、用户操作与自动状态流转、正向与相关异常路径。未决分支关联 PRD 的 Q 编号，不擅自补成规则。只有交互或布局明显不同的状态单独画屏，其余在说明中交代。
4. **生成**：运行 `node <SKILL>/scripts/pm-draw.mjs validate <flow.json>`，再 `build <flow.json> --out <site目录>`。产物为 `index.html` 总索引和每个流程组一个 `<group-id>.html`，不逐屏拆文件。参考截图在索引集中展示，每屏链接到相关参考。

小清单直接写 JSON。清单较大时用 `init` → `add-requirements` → 逐个 `add-group` 分块组装，最后统一校验；命令见 flow-format。参考截图采集受阻时说明具体缺口并继续独立工作，不用线框冒充竞品截图，也不声称调研已完成。

### 根据反馈修改

接收 HTML 导出的 JSON / Markdown、用户粘贴的反馈或对话指示。沿用页面 id 与 Q 编号定位，只处理相关范围：

- **表达修改**：调整层级、布局、文案表达或交互说明；业务含义不变则直接改 flow 并重建。
- **回答决策**：将用户选择写回 PRD 正文与决策记录，再按审查状态同步原型。回答问题不会自动批准整份文档；若答案改变已通过的业务规则，按业务变化回退状态。
- **业务变化**：先写回 PRD，说明影响哪些页面、规则或状态并回退状态；用户要求预览才同步草稿，正式方案等重新通过。未受影响的设计和截图参考保留。
- **含糊或冲突**：给出具体问题与选项，等答案后再改依赖部分；其余修改继续。

保留用户反馈原话，不能把“建议”自动当成新需求批准。用户明确要求采纳的业务建议可直接写入草稿 PRD，不重复询问是否允许修改。

有匹配当前 HTML 的反馈 JSON 且需要保留上一轮时，在修改 flow / PRD 前执行：

```bash
node <SKILL>/scripts/pm-draw.mjs next-round <flow.json> --feedback <feedback.json> --out <round-2/flow.json>
```

修改 PRD 前先复制到新轮次并更新 `prd.path`，保留旧依据。无 JSON 时仍可按文字反馈局部修改；有需保留的旧产物则先备份，不强制导出、开新轮或重做竞品研究。反馈版本不一致时先核对对应页面与现行 PRD，不能伪造 fingerprint 通过导入。旧 HTML 是对应 PRD 版本的快照，PRD 变更后不要把它当当前正式方案。

## 交付

按 [references/review-checklist.md](references/review-checklist.md) 检查本次实际改动，这只是产物自检。交付已生成文件链接、简短修改说明和仍需用户选择的问题。HTML 保留每屏标记、问题、建议及分组反馈，可在当前浏览器保存并导出 JSON / Markdown；提醒需要跨会话使用时导出即可，不要求用户完成额外走查。
