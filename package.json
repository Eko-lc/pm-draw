{
  "name": "pm-draw",
  "version": "1.0.0",
  "private": true,
  "description": "PRD、低保真原型与产品流程走查 Skill",
  "type": "module",
  "scripts": { "test": "node --test scripts/*.test.mjs" },
  "engines": { "node": ">=22" }
}
