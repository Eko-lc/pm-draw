---
name: pm-draw
description: 根据需求编写八章 PRD，基于 PRD 生成低饱和功能配色的低保真 HTML；产品已有页面时，先列流程分组、页面和状态供用户确认，再采集或接入截图，与设计原意并排走查，保存反馈并跨轮回归。适用于写 PRD、画原型、页面走查和原型方案评审。
---

# PM Draw

以 PRD 为依据，把需求、低保真原型和实际页面连成可走查的流程。脚本处理渲染、截图接入与反馈数据；PRD 阅读、流程提取和原型构思由当前 Agent 完成，不把关键词解析器当成需求理解。

`<SKILL>` 指本文件所在目录，命令都用解析后的实际路径。运行依赖 Node.js 22+，无需 npm 安装、外部字体、图标或在线服务。交付物放在用户指定目录；没有约定时用当前项目的 `designs/<product>/`。在本仓库工作可直接读取本文件；作为 Skill 使用时整个目录一起安装。

## 必须保留的边界

- 不擅自增加 PRD 没有的功能、文案规则或业务分支。PRD 未明确处写「待确认」，与已确认需求区分；示例数据明确是示意数据。
- PRD 的「已审查通过」只能来自用户本次明确表达；不自行定稿、不替用户选择决策项。用户口头表示通过后，可代为写入状态行并注明日期。
- 低保真用中性灰加不超过 5 种低饱和功能色（雾蓝＝主操作与强调、灰绿＝通过、灰琥珀＝待确认与改动、砖红＝异常与截图标注）、基础框线、图片占位块，表达信息层级、核心文案、按钮位置和关键状态；颜色只用于功能区分、状态提示、层级强调，不做视觉美化。实际截图保留产品原貌；截图上的标注编号、圆点与箭头只是方案标注覆盖层，不修改原图，也不给线框引入新颜色。绘制与标注细节遵循 [references/wireframe-guide.md](references/wireframe-guide.md)。
- 按真实用户使用顺序分组，一屏对应一个页面状态、一份原型和一份原型说明。原型说明写清页面逻辑、布局分区、组件行为与交互跳转，PRD 原文作为依据折叠保留、可追溯。正常路径、重要分支及 PRD 涉及的首次使用、权限、加载、空数据、失败、恢复等状态分别呈现。不把未提及的状态自动变成产品功能。
- 已有产品地址或可运行环境时，**先不运行产品、不连接浏览器、不截图**。先根据 PRD 列出准备覆盖的流程分组、页面、状态和复现方式，展示计划，等待用户确认。确认后才执行，范围变化则重新确认。用户已明确确认同一份计划时不重复询问。
- 本轮反馈按“页面—状态—问题—建议”记录。上一轮原话只读保留，不改写、总结替换或当成新需求；反馈引出的新功能须先由用户明确纳入 PRD。

## 协作阶段

本 Skill 对应一个分阶段、可来回的协作流程，不是单向流水线：

1. **用户给输入**：需求背景和目标；新功能描述逻辑和操作路径，旧功能迭代描述旧逻辑和新逻辑。
2. **AI 写 PRD**：编写完善 PRD；未能确定的事项不擅自假设，汇总为带选项的决策记录请用户决策（格式见 prd-writing 的决策记录）。
3. **用户审查定稿**：用户审查 PRD、回答决策，AI 按回答写回正文；可来回多轮，直到用户在 PRD 文首标记「状态：已审查通过」。
4. **AI 出方案**：依据定稿 PRD 绘制原型；有实际页面时进入对照走查。
5. **灵活回流**：原型 HTML 的标记、问题和建议，或走查反馈，可回流到任意阶段——表达问题直接改清单重建，需求变化回阶段 2 改 PRD 并重新定稿。规则见「反馈回流与新一轮」。

各阶段可跨多轮对话进行，不要求一次完成；用户可能只带阶段 1 的粗输入来，也可能直接带已定稿 PRD 来。先判断当前处于哪个阶段，再按下述对应路径工作。

## 按输入选择工作路径

### 写或补 PRD：决策与定稿

读取 [references/prd-writing.md](references/prd-writing.md)，以其中引用的用户八章规范写 `PRD-<product>.md`，文首写状态行。已提供 PRD 则先读完整内容，按用户范围修改，不能为了套模板丢掉现有需求。联系人、指标、排期、业务规则没有依据时标「待确认」。

未确定事项集中到 7.4 决策记录：每条给出问题、背景、互斥选项及影响、有依据时的 AI 建议，请用户选择；不要把决策埋进正文让用户找。用户回答后把结果写回正文、消除对应待确认，决策记录标「已决策」并保留。

每轮交付 PRD 时汇报：当前状态、本轮修改点、剩余待决策问题列表。用户明确审查通过后，把状态行改为「已审查通过（日期）」；此后任何需求性修改先改回「草稿」，重新审查。产品网址可记录为输入，但进入走查流程前不访问产品截图。

需要原型时，在第 7 章记录流程 / 页面状态表、操作与跳转、关键文案、状态分支、待确认项和验收条件，再进入下述路径。

### PRD 已定稿：绘制原型

前置：PRD 文首状态为「已审查通过」。用户明确要求先看草稿方案时可提前绘制，但生成的 HTML 会标注 PRD 未定稿、仅供讨论；正式方案仍以定稿 PRD 为依据。

1. 阅读 [references/flow-format.md](references/flow-format.md)。提取带 PRD 原文引用的 `requirements`，创建 `mode: prototype`、`target.kind: none` 的流程 JSON。**PRD 较大（分组多、页面状态多）时不要一次性写出整个流程 JSON**——单次输出过长会被截断、导致生成中断。改用分块组装：先 `node <SKILL>/scripts/pm-draw.mjs init <flow.json> --prd <PRD.md> --project <id> --title <名称>` 建骨架，再 `add-requirements --file <需求数组.json>` 加入需求，然后每个流程分组各写一个小片段文件、逐个 `add-group --file <分组.json>` 追加，最后 `validate` 做全局校验（中间态允许需求暂未覆盖、跳转暂未闭合）。每个片段文件都足够小，可在单次回复内安全写出。
2. 每屏有稳定 id、页面名称、当前状态、复现 / 进入方式、PRD 来源、线框组件、可执行操作和跳转目标；并写「原型说明」：`summary` 概述本屏任务与进入条件，`layout` 按区域说明布局分区与各区职责，组件级 `note` 说明每个可感知元素展示什么、如何操作、发生什么及数据 / 校验规则（渲染时自动编号、与线框联动），`logic` 列条件展示、边界与异常等业务规则。写法规范见 [references/wireframe-guide.md](references/wireframe-guide.md) 的「原型说明写法」。信息层级用 heading 1–3 级表达；每屏最多一个主操作（`role: primary`）；操作写明 outcome 与跳转目标。所有核心状态在一个 HTML 中平铺；未知规则和跳转显式标「待确认」。组件都引用需求编号或「待确认」。
3. 执行 `node <SKILL>/scripts/pm-draw.mjs validate <flow.json>`，修正来源、覆盖和跳转错误；validate 同时报告 PRD 状态。再执行 `build <flow.json> --out <输出目录>`：生成 `index.html` 总索引和每个流程分组一个 `<group-id>.html` 子页面。拆分只到流程分组，不把单屏拆成独立页面——目的是后续修改变更时快速定位，拆太细反而难维护。跨组的操作跳转和上一屏 / 下一屏导航自动生成带页面文件名的相对链接。
4. 按 [references/review-checklist.md](references/review-checklist.md) 检查 PRD 的语义覆盖、线框信息层级、关键状态及本地打开效果。交付 `index.html` 入口与分组子页面，并提供 PRD 和流程 JSON 供后续迭代。

### 已有真实页面：截图与原型对照

1. 创建 `mode: review` 清单。已有手动截图用 `target.kind: manual`；产品地址用 `url`；可运行环境用 `runtime`，记录启动方式及访问地址。先基于 PRD 画设计原意，不从截图倒推、照抄实际界面当作设计原意。
2. 对 `url/runtime`，执行 `plan <flow.json> --out <walkthrough-plan.md>`，向用户展示完整流程分组、页面、状态、复现方式、待确认项。停在此步骤，等用户确认后才继续。确认源自用户本次要求，不能把建立工具包的授权当成日后目标产品截图的授权。
3. 收到确认后，用计划输出的摘要和用户原话执行 `approve <flow.json> --hash <摘要> --quote <确认原话>`。用户只确认部分页面时附加 `--screens <id1,id2>`，将执行范围限制到这些页面。此命令只记录确认，不会替你询问用户；严禁自动写“用户已确认”等虚构证据。PRD 或计划语义变化后需重新展示并确认。
4. 读取 [references/browser-capture.md](references/browser-capture.md)。实际运行环境，按确认的复现步骤操作产品，验证目标页面状态，然后截图。可用已连接的浏览器工具、web-access Proxy，或内置 CDP 适配器。手动上传截图先检查实际内容，再 `import-shot` 绑定页面状态。所有方法都保留来源、实际状态、时间、复现方式；失败或无法复现要记录 `blockedReason`，不能伪造截图或用线框冒充。
5. 为每张截图匹配对应的 PRD 低保真原型；填 `annotations` 记录“原逻辑—PRD 依据—修改点”。用 `rect` 红色编号矩形圈区域、`pin` 红色圆点标控件、`arrow` 红色箭头连线指向相关区域，编号与说明列表一一对应。没有观察到的原逻辑标「待确认」，不凭 PRD 猜测实际行为。
6. 执行 `build <flow.json> --out <输出目录>`。总索引给出各组截图接入进度；分组子页面桌面并排显示实际截图和设计原意，每屏提供「符合预期」「需要调整」「严重问题」及问题 / 建议输入框，每组末尾有流程反馈。缺失截图会明确展示未完成，不算已完成走查。
7. 告知用户反馈在当前浏览器自动保存、同轮各页面共享，任意页面导出的 JSON 都包含全部分组反馈，可导出 Markdown 和 JSON。不要声称浏览器缓存能永久保存或跨浏览器同步。让用户将 JSON 导出作为下一轮输入。

### 反馈回流与新一轮

拿到原型或走查 HTML 的反馈（每屏标记、问题、建议；用户导出的 JSON / Markdown，或对话中的原话）后，先逐条分类再动手：

- **表达类**：PRD 已明示，但原型的层级、文案、布局、状态呈现不对——直接修改本轮清单，`validate` 后 `build` 重建，不动 PRD。
- **决策类**：反馈把某个「待确认」变成了确定答案——先写回 PRD 决策记录和正文（标「已决策」），再同步清单并重建。
- **需求类**：新功能、规则变化、与 PRD 冲突的期望——先回 PRD 路径：更新对应章节，状态行改回「草稿」，向用户说明变更点，重新审查通过后再同步清单 requirements / quote 并重建。不绕过 PRD 直接把新需求画进原型。

纯表达类小改可直接重建；想把本轮反馈原话保留进历史、开新轮次时，先保留当前清单和 HTML，取得与其匹配的反馈 JSON，再执行：

```bash
node <SKILL>/scripts/pm-draw.mjs next-round <flow.json> --feedback <feedback.json> --out <round-2/flow.json>
```

然后在新清单中修改设计。PRD 也要变化时，先复制一份到新轮次并更新 `prd.path`，保留上一轮依据。页面状态 id 不变，新增状态用新 id，删除的页面历史仍在“全部历史反馈”中。脚本把原话原样带入历史，清空本轮输入及旧截图，要求重新采集。内容摘要比较原型、标注、页面信息、复现方式、操作、流程所属和截图字节，自动标记「已改动 · 待回归」「新增页面状态」或「与上一轮一致」。`changeSummary` 说明本轮实际改了什么；不把页面改动标签当成已修复结论。截图中动态时间等变化也会触发改动，需人工回归判断。

新轮次的产品截图计划仍须确认；单纯页面反馈输入无需确认。原始 JSON 和上一轮 HTML 都不覆盖。

## 交付检查

读取 [references/review-checklist.md](references/review-checklist.md)，完成适当检查后给出实际生成的文件链接、覆盖范围、缺失截图 / 待确认项及反馈导出方式。修复工具或用仓库内虚构样例做自动化验证不属于对用户真实产品执行走查，不需要套用目标产品截图确认步骤。
